apiVersion: v1
kind: Service

metadata:
  name: keycloak-service
  namespace: revenda

spec:

  type: NodePort

  selector:
    app: keycloak

  ports:

    - port: 8080
      targetPort: 8080
      nodePort: 30081